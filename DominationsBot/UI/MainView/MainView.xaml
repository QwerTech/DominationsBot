<Window x:Class="DominationsBot.UI.MainView.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:DominationsBot"
        xmlns:viewModel="clr-namespace:DominationsBot.UI.MainView"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d"

        Title="MainWindow" Height="350" Width="525" d:DataContext="{d:DesignInstance viewModel:MainViewModel, IsDesignTimeCreatable=True}">
    <Grid>
        <TextBlock Text="{Binding Path=ImageName}"/>
        <Image Source="{Binding RenderImage}" />
    </Grid>
</Window>